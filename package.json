{
  "name": "tax-advisor-app",
  "version": "1.0.0",
  "description": "AI-powered tax advisor application for web and mobile platforms",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:web\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:web": "cd frontend/web && npm start",
    "build": "npm run build:backend && npm run build:web",
    "build:backend": "cd backend && npm run build",
    "build:web": "cd frontend/web && npm run build",
    "test": "npm run test:backend && npm run test:web",
    "test:backend": "cd backend && npm test",
    "test:web": "cd frontend/web && npm test",
    "setup": "npm install && cd backend && npm install && cd ../frontend/web && npm install"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YOUR_USERNAME/tax-advisor-app.git"
  },
  "keywords": ["tax", "advisor", "fintech", "react", "nodejs"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "nodemon": "^3.0.1"
  }
}
